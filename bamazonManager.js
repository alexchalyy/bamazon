/*  This program when executed asks the user to either view products for sale (items' ids, names, prices, and quantities), view low inventory (quantity lower then 5), add to inventory (add quantity), and add new product.

const mysql = require("mysql");
const Inquirer = require("inquirer");